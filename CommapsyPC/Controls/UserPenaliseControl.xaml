<UserControl x:Class="CommapsyPC.Controls.UserPenaliseControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CommapsyPC.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="2*"/>
            <RowDefinition Height="2*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Name="background" Grid.RowSpan="3" Background="#903749" CornerRadius="40" Margin="10"/>

        <Label Name="userName" Content="Nombre del usuario" HorizontalContentAlignment="Center"
               VerticalContentAlignment="Center" FontSize="30"/>

        <TextBox Name="reply" Grid.Row="1" TextWrapping="Wrap"
            AcceptsReturn="True"
            VerticalScrollBarVisibility="Visible" Background="White" Margin="20"/>

        <Button Name="Penalise" Click="Penalise_Click" Content="Penalizar" Grid.Row="2" Grid.Column="2" Margin="300,0" Background="#E84545"/>

    </Grid>
</UserControl>
